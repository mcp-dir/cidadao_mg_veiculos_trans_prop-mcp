# Ferramentas

Cidadão MG Veículos: Transferência de Propriedade de Veículo expõe 1 ferramenta (todas somente leitura).

### 1. `cidadao_mg_veiculos_trans_prop_consultar`
**Input**: `login_cpf` (opcional), `login_senha` (opcional), `pkcs12_cert` (opcional), `pkcs12_pass` (opcional), `placa`, `chassi`, `renavam`, `valor_venda`, `crv`, `quilometragem_hodometro` (opcional), `data_hora_hodometro` (opcional), `cpf_vendedor` (opcional), `cnpj_vendedor` (opcional), `email_vendedor`, `cpf_comprador` (opcional), `cnpj_comprador` (opcional), `nome_comprador`, `email_comprador`, `cep_comprador`, `uf_comprador`, `municipio_comprador`, `logradouro_endereco_comprador` (opcional), `bairro_endereco_comprador` (opcional), `numero_endereco_comprador`, `complemento_endereco_comprador` (opcional)

Cidadão MG Veículos: Transferência de Propriedade de Veículo, consulta em fonte oficial.

## Prompts de exemplo

```
Consultar Cidadão MG Veículos: Transferência de Propriedade de Veículo
```
